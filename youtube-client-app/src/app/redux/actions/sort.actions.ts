import { createAction } from "@ngrx/store";

export const sortByDate = createAction("[SortSetting Component] sortByDate");

export const sortByViewCount = createAction(
    "[SortSetting Component] sortByViewCount"
);
